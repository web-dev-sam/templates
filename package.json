{
  "name": "webry-monorepo-template",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "workspaces": ["apps/docs", "apps/web"],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "check": "biome check",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.3"
  }
}
