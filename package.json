{
  "name": "webry-monorepo-template",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/docs",
    "apps/web",
    "configs/prettier"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "prettier": "./configs/prettier/.prettierrc.json",
  "devDependencies": {
    "oxlint": "^0.9.9",
    "prettier": "^3.3.3"
  }
}
